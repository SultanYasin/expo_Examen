exprot default class Todo {
    constructor(id , title ,number , times, isCompleted){
        this.id = id, 
        this.title = title,
        this.number = number,
        this.times = times,
        this.isCompleted = isCompleted
    }
}