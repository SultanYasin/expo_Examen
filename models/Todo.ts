exprot default class Daily_Exercises {
    constructor(id , title ,number , times, isCompleted){
        this.id = id, 
        this.title = title,
        this.times = times,
        this.isCompleted = isCompleted
    }
}